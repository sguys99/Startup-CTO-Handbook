{
  "name": "startup-cto-handbook-ko-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static e-book site for the Korean translation of The Startup CTO's Handbook",
  "scripts": {
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('../dist',{recursive:true,force:true}))\"",
    "build:html": "node build.mjs",
    "build:search": "pagefind --site ../dist",
    "build": "npm run clean && npm run build:html && npm run build:search",
    "serve": "serve ../dist -l 4173",
    "preview": "npm run build && npm run serve"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "pagefind": "^1.1.1",
    "serve": "^14.2.3"
  }
}
